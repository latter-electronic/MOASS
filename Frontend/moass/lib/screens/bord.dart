import 'package:flutter/material.dart';

class bordScreen extends StatelessWidget {
  const bordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Work'));
  }
}
