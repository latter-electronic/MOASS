pipeline {
    agent any

    stages {
        stage('Embedded Build') {
	        steps {
				dir('Embedded/electron-app') {
                    sh 'docker build -t embedded .'
                    sh 'docker tag embedded dongho416/embedded'
                    sh 'docker push dongho416/embedded'
                }
            }
        }
    }
}
